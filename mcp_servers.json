{
  "servers": {
    "filesystem": {
      "enabled": true,
      "command": [
        "{node}",
        "{filesystem_server}",
        "{workspace}"
      ]
    },
    "git": {
      "enabled": true,
      "command": [
        "{python}",
        "-m",
        "mcp_server_git",
        "--repository",
        "{workspace}"
      ],
      "environment": {
        "GIT_CONFIG_COUNT": "1",
        "GIT_CONFIG_KEY_0": "safe.directory",
        "GIT_CONFIG_VALUE_0": "{workspace_posix}"
      }
    },
    "network_ops": {
      "enabled": true,
      "command": [
        "{python}",
        "-m",
        "src.network_server"
      ]
    },
    "network_ops_remote": {
      "enabled": false,
      "transport": "streamable_http",
      "url_env": "MCP_REMOTE_URL",
      "auth_token_env": "MCP_REMOTE_TOKEN"
    }
  }
}
